name: example
description: A new Flutter project.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  morozteam_ui:
    path: ../

flutter:
  uses-material-design: true

  assets:
    - assets/icons/
    - assets/images/
